server:
  port: 8010
spring:
  application:
    name: microservice-consumer-movie
  cloud:
    service-registry:
      auto-registration:
        enabled: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/

